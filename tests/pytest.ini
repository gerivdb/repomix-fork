[tool:pytest]
addopts = -n auto --dist=loadscope -m "not integration"
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests requiring external dependencies (NEXUS, src.*, etc.)
    tdd: marks tests following test-driven development patterns